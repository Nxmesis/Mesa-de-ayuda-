<%- include('partials/head') %>
<%- include('partials/sidebar') %>

<div class="main-content">
  <%- include('partials/navbar') %>

  <div class="container">
    <div style="max-width:800px; margin:0 auto;">
      <h1 style="color:#2d3748; margin-bottom:25px;">Nueva Solicitud</h1>

      <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
        <div class="alert alert-danger"><%= error[0] %></div>
      <% } %>

      <div class="card">
        <div class="card-header">Información</div>

        <form method="POST" action="/tickets" enctype="multipart/form-data">

          <div class="form-group">
            <label class="form-label">Título de la novedad *</label>
            <input type="text" name="titulo" class="form-control"
                   placeholder="Describe brevemente el problema" required>
          </div>

          <div class="form-row">
            <div class="form-group">
              <label class="form-label">Tipo de Incidente</label>
              <select name="categoriaId" class="form-control">
                <option value="">Selecciona un tipo...</option>
                <% if (typeof categorias !== 'undefined' && categorias && categorias.length > 0) { %>
                  <% categorias.forEach(cat => { %>
                  <option value="<%= cat.id %>"><%= cat.nombre %></option>
                  <% }) %>
                <% } %>
              </select>
            </div>

            <div class="form-group">
              <label class="form-label">Prioridad *</label>
              <select name="prioridad" class="form-control" required>
                <option value="" disabled selected>Selecciona la prioridad...</option>
                <option value="Baja">Baja — Puedo trabajar con normalidad</option>
                <option value="Media">Media — Tengo alguna alternativa</option>
                <option value="Alta">Alta — Afecta mi trabajo significativamente</option>
                <option value="Critica">Crítica — No puedo trabajar</option>
              </select>
              <div class="form-hint">Selecciona la que mejor describa el impacto del problema.</div>
            </div>
          </div>

          <div class="form-group">
            <label class="form-label">Equipo afectado</label>
            <input type="text" name="equipo" class="form-control"
                   placeholder="Ej: Caja1, Caja2, etc.">
          </div>

          <!-- Checkbox Solicitar Llamada -->
          <div class="form-group" id="llamadaBox" style="background:#fffbeb; padding:18px; border-radius:8px; border:2px solid #f6e05e; margin-bottom:20px;">
            <label style="display:flex; align-items:flex-start; gap:12px; cursor:pointer; margin:0;">
              <input type="checkbox" name="solicitarLlamada" id="chkLlamada" style="width:20px; height:20px; margin-top:2px; accent-color:#d69e2e;">
              <div>
                <div style="font-weight:600; color:#744210; font-size:1.05rem;">📞 Solicitar llamada telefónica</div>
                <div style="color:#975a16; font-size:0.9rem; margin-top:4px;">
                  Si activas esta opción, alguien del personal administrativo te llamará para atender tu solicitud.
                  No necesitas escribir una descripción detallada.
                </div>
              </div>
            </label>
          </div>

          <div class="form-group" id="grupoDescripcion">
            <label class="form-label">
              Descripción detallada <span id="reqDesc" style="color:#e53e3e; font-weight:600;">*</span>
            </label>
            <textarea name="descripcion" id="txtDescripcion" class="form-control" rows="5"
                      placeholder="Describe el problema con el mayor detalle posible: ¿cuándo comenzó?, ¿qué sucedió?, ¿qué estabas haciendo cuando ocurrió?"
                      required></textarea>
          </div>

          <div class="form-group">
            <label class="form-label">Adjuntar imagen (opcional)</label>
            <input type="file" name="imagen" class="form-control"
                   accept=".png,.jpg,.jpeg,.gif,.webp">
            <div class="form-hint">Puedes adjuntar una captura de pantalla del error. Máx 16MB.</div>
          </div>

          <div class="page-actions">
            <button type="submit" class="btn btn-success">Enviar Solicitud</button>
            <a href="/dashboard" class="btn btn-secondary">Cancelar</a>
          </div>

        </form>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</div>